英语阅读训练 Demo 项目任务进度（更新于 2026-05-03）

项目目录：C:\Users\16098\Desktop\chicken-baby
当前页面：demo.html
备份页面：demo1.html（已保留原版本）

一、当前完成情况总览
- 已完成：任务 1、任务 2、任务 3、任务 4、任务 5、任务 6
- 本次新增完善：任务5样式与交互二次迭代（弹窗优化 + 英/美双发音）
- 当前未完成：任务 7

二、本次新增完善内容
1) 音频文件接入兼容
- 已兼容两种文件命名：
  - `audio-us.mp3` / `audio-us.mp3.mp3`
  - `audio-uk.mp3` / `audio-uk.mp3.mp3`
- 页面会自动尝试候选文件名，避免因双 `.mp3` 后缀导致播放失败。

2) 美音/英音切换体验优化
- 保留口音切换按钮（美音/英音）。
- 整段播放时切换口音，支持按当前进度比例无缝续播（目标音频可用时）。
- 若目标音频尚未就绪，会在加载后自动恢复续播。

3) 播放状态与提示完善
- 音频加载中显示“音频加载中...”。
- 音频缺失时显示明确文件名提示。
- 复读、倍速、单句播放与翻译展开功能保持可用。

4) 重点单词与释义弹窗二次优化（任务5迭代）
- 重点单词样式调整为：黑色字 + 橙色下划线。
- 弹窗视觉优化：宽度加大（320px+）、`padding` 增大、圆角 14px、阴影增强。
- 弹窗信息结构优化：
  - 顶部展示单词（加粗放大）
  - 英音标一行 + 独立喇叭按钮
  - 美音标一行 + 独立喇叭按钮
  - 下方展示“词性 + 中文释义”（多词性分行）
- 发音逻辑改为本地音频播放：
  - 英音：`../重点词汇/{word}_audio-uk.mp3`
  - 美音：`../重点词汇/{word}_audio-us.mp3`
- 交互反馈：喇叭默认灰色，hover 变紫并轻微放大，点击有按压反馈，播放中有脉冲动画。
- 关闭逻辑：点击浮层外区域或按 `Esc` 关闭。
- 预加载优化：鼠标悬停重点词时预加载该词英/美音频。
- 特殊命名适配已加入：
  - `grandma's` -> `grandmas`
  - `West Africa` -> `West_Africa`

5) 本次补充修正（2026-05-03）
- 已完成重点词音频完整性核对：当前页面词表对应音频缺失数为 0。
- `smoked` 与 `beef` 已改为分开读（不再复用 `smoked_beef` 合并音频）。
- 弹窗细节微调：
  - 喇叭与音标距离缩小（更贴近）
  - 中文释义字号上调（更易读）

6) 二维码生成与悬浮展示（任务6）
- 已接入二维码库：`qrcodejs`（CDN 引入）。
- 标题栏新增“扫码”按钮，支持悬浮显示二维码卡片。
- 二维码默认指向当前页面链接（`window.location.href`）。
- 支持通过页面属性覆盖配置：
  - `data-qr-link`：二维码目标链接
  - `data-qr-title`：二维码标题文案
- 交互完善：
  - 鼠标移入按钮显示，移出延时隐藏，移入卡片保持显示
  - 点击按钮可切换显隐
  - 点击卡片外区域或按 `Esc` 可关闭

三、任务状态表
------------------------------------------------------------
序号 | 任务名称                           | 状态       | 说明
------------------------------------------------------------
1    | 基础页面结构 + 顶部标题栏          | 已完成     | 已实现渐变标题栏、返回按钮、内容区基础结构。
2    | 底部播放器 UI + 播放/暂停          | 已完成     | 播放器 UI 与交互已完成。
3    | 音频播放逻辑（整段/单句/复读/倍速）| 已完成     | 已支持真实音频+单句播放+复读+倍速。
4    | 文本区 + 翻译展开                  | 已完成     | “译”按钮展开/收起完成。
5    | 单词高亮标注                       | 已完成     | 已接入 JSON 渲染、高亮样式、优化弹窗、英/美双喇叭本地发音，且音频资源齐全。
6    | 二维码生成                         | 已完成     | 已接入二维码库，支持悬浮展示、点击切换与外部点击/Esc关闭。
7    | 打包 + 文档（README/template）     | 未完成     | 未整理最终交付文档与模板页。
------------------------------------------------------------

四、当前剩余工作
- 任务7：补齐 README.md 与 template.html，完成交付包装。

五、建议下一步执行顺序
1) 做任务7（文档与打包收尾）。
